# 2026-09-22 — 実際に遊べるタッチ版と説明書

## 実装

- キーボード／マウス／タッチの操作モード。キーボードの矢印視点・J射撃・K補助・L水平復帰。pointer lockはマウスモードのみ。
- 独立したpointer captureで左スティックと右視点、押下射撃中のドラッグ。移動軸の正規化、入力源別の押下管理、cancel時の解除。
- 状態に応じた水色の操作、運搬・牽引・上昇・下降・加速。補助操作はポーズメニューから選択。
- 任意の遮蔽を考慮した照準補助。敵の体力・攻撃・ミッションのルールは共有。
- 可読性の高いHUD、左利き、大きい文字、軽量初期設定、安全領域。画面回転・背景移行は保存して停止。
- 設定v2へ移行、旧設定の基本値を保持。ミッション保存形式はv1。
- public/help.htmlにスマホ向け操作説明・取引ルート攻略・復旧方法。ゲーム内から開ける。

## 検証

- 25単体テスト成功。ビルド・Lint成功。docs/evidence/mobile/source-checks.jsonに出力と本番ファイルのハッシュ・容量。
- Chrome、844×390、touch emulationで戦闘・潜入・取引・混合を新規開始からクリア。全操作はタッチイベント。開発APIは位置・視点・状態の読み取りにだけ使用し、位置・視点・missionへの書き込みなし。潜入と取引は発砲なし。
- 取引1.7分、潜入/戦闘2件の成功は初回3件実行のうちの2件。混合の初回は編集中のVite再読み込みで中断したため、ソースを固定して再実行し1.6分で成功。時間は自動試験の実時間で、人間の初回プレイ時間ではない。
- 保存前後・破損保存・戦闘/装填・1440/1000pxの起動/設定/復帰・844/667pxのタッチUI/回転/設定保存・マウス固定なしの視点/射撃を確認。初回選択回帰8/8、最後のUI修正後3/3。
- 本番プレビューで起動・タッチUI・停止・説明書の表示を確認。390px幅の説明書に横スクロールなし、コンソール例外0。開発操作APIは本番JSから除外。
- 2指で移動・照準ドラッグ・射撃を同時に行い、指を離すと射撃が停止することを追加確認。
- 証拠：docs/evidence/mobile/。PC基準時の既存画像は復元し、この版の画像を別ディレクトリに保持。

## 未確認・次段階

実際のiPhone/Androidの機種別性能・Safari挙動・発熱・メモリ・2周連続の実機記録、初見の人の体験は未検証。PWA/offline・ネイティブ化・自由なキー再割当・タッチ配置の任意拡大は未実装。設計v2の全リリース条件を満たしたとの主張はしない。今回の到達点は、タッチ入力で全ルートを通せるオンラインWeb版と、その説明書・配信。

## 配信確認

2026-09-22 08:36 JST、配信サービスが成功（succeeded）を返した。確定したゲームURLは https://heart-heist-browser.ryoakg.chatgpt.site 、説明書は同URLの /help.html 。本人用のアクセス範囲を維持。配信対象ソースは 6359546fbeec6cd9ee76cb0c1679a2de001890ab。本番URLのブラウザ操作による再検証は行っておらず、配信サービスの成功応答とローカル本番ビルドの検証を区別する。
